<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A simple and responsive mini message board application.">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Messageboard</h1>
    <ul>
        <% messages.forEach((msg, index) => { %>
            <li class="message-card">
                <div class="message-header">
                    <strong><%= msg.user %></strong>
                    <small>(<%= msg.added.toLocaleString() %>)</small>
                </div>
                <p class="message-text"><%= msg.text %></p>
                <div class="message-actions">
                    <a href="/message/<%= index %>" class="button-link">Open</a>
                </div>
            </li>
        <% }); %>
    </ul>
    <div class="button-container">
        <a href="/new" class="button-link">Add a new message</a>
    </div>
</body>
</html>
